/*import { Offer } from '../actions'

const offer = (state = Offer.SHOW_ALL, action) => {
  switch (action.type) {
    case 'CREATE_NEW_OFFER':
      return action.filter
    default:
      return state
  }
}

export default offer
*/